# Database
DATABASE_URL="postgresql://pro_casa_user:CHANGE_ME@postgres:5432/pro_casa_db?schema=public"

# JWT Secret (generate with: openssl rand -hex 32)
JWT_SECRET="CHANGE_ME_STRONG_SECRET"

# Server
PORT=3001
NODE_ENV=production
API_URL=https://pro.casa.kz

# AI API Key (also configurable via Admin Panel → Settings)
# Leave empty - key is managed via admin panel
DEEPSEEK_API_KEY=""
